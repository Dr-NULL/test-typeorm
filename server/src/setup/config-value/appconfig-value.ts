import { AppConfigData } from '@tool/config';

export const appConfigValue: AppConfigData = {
    port: 80,
    paths: {
        client:     '../client',
        sessions:   '../data/sessions',
    }
};